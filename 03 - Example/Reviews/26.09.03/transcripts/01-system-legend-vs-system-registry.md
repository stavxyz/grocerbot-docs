# DRAPER Doc Review - 1 -- System Legend vs System Registry

> Voice memo, September 3, 2026. One speaker: Buck. Verbatim transcription (4.9 minutes).

DRAPER visual documentation review of Onboarding.

We've got over on the left something that says decisions, pieces without components. Okay, so these are things that don't have a component on the System Legend. So interface, lead qualification form.

So the System Legend is meant to have, like, interface, and define interface, and the colors we use, and generally the shapes and standards of the many different interfaces that we're going to create that are part of the system. The lead qualification form is one interface, right? That would go on the System Registry, which is where we take all of the, what are we calling them? All of the different levels, the different pieces from the different levels. And not just the levels, because like actions, we should define all the actions here, but it's like all of the interfaces, all of the artifacts, all of the functions, all of the logic, all of the actors, all of the actions, all of the interfaces, artifacts, I said, all of the piping things, all of the data things.

They'll show up here in, like, a list with, like, the name that we use within our system, the coded name, like the proper normal human language name, proper sentence case, I guess. Or no, no, no, no, no, title case. A little description, where it exists, where it's hosted, and then where it shows up in the system.

And then right now in the System Registry, we have these, like, each of the functions, right? They're kind of going out. I mean, it's just a grid of functions. I want it going in a horizontal line from FN-0001 all the way out to the right, each one next to each other. And they shouldn't look like these card things that we have here. They should literally look like what they look like when they show up in the actual phases. For example, the interface for the public qualification form will have that designed here, like fully designed, right?

The other thing I'm noticing, and maybe we're just going to come back to that. It's fine. But I think we should at least lay them out horizontally and make them roughly like the size of our usual function things or logic things or whatever, so that we can see them.

And then I'm just noticing, like, data registry, but then we have an input registry versus an output registry, how those differ. And then I'm seeing Draper as a host. Okay, that makes sense. For interfaces and artifacts, yeah. All the actions are effectively basically in code, right? Or are in the harness code or in the function shit. Okay, ignore those last two things.

But yeah, I want them kind of going out horizontally to the left. And we'll do the full designed version there. That way we won't have instances or things like this lead qualification form where it's like, oh, this doesn't exist in the System Legend. Well, that's because it's not supposed to. Interfaces, I think, is well defined in the System Legend. Let's see what we have there. Oh, do we not? No, we do. In the Process level. Yeah, we have it defined there. And we even use the fucking lead qualification form as the example to show an interface, the stylings of interface, in the System Legend. So I don't know what it means by this doesn't exist there.

Same thing, like it's saying that the Notion CMS mock doesn't exist in the interface. It shouldn't. It should exist in the System Registry. Same with the WhatsApp message. And then same with, I think, all these things.

What is a runner? And then unruled colors. If it's, yeah, they should just be fixed. So I don't know why that's there.

Anyways, this started as a review of Onboarding, but it turned into a System Legend versus System Registry. So I'm going to cut it here. Okay.
